package com.zero.android.navigation

import com.zero.android.common.util.WEB_URL

object DeepLinks {
	private const val URI = WEB_URL

	const val CHANNEL = "$URI/channel={channel_id}&is_group_channel={is_group_channel}"
}
